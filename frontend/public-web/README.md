# Public Web

Ứng dụng Next.js độc lập dành cho học viên.

## Chạy local

```bash
npm install
npm run dev
```

Sao chép `.env.example` thành `.env.local` và chỉ dùng biến `NEXT_PUBLIC_*` không chứa secret.

## Kiểm tra

```bash
npm run lint
npm run type-check
npm test
npm run build
```
